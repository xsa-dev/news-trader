# TODO:
# start ccxt-proxy for translace trades to exchange (if needed)
# get all wallets from active blockchain contracts
# get all profiles from supabase
# send signal on profile (user can set all)
# profile can connect to best exchange services
# send signal to exchange (if user set)
# decrease user signals count balance
